{
  "name": "electric-particles-exercise-start",
  "version": "1.0.0",
  "description": "Starter for electric-particles exercise in p5 and ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "devDependencies": {
    "@types/p5": "^1.4.2",
    "browser-sync": "^2.27.9",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  }
}
